package com.webalo.iliad.Controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/profile")
public class PersonalDataController {

    /*
     * endpoint per la modifica del metodo di pagamento @PostMapping("/update-payment}")
     * endpoint per la modifica della password @PostMapping("/update-password}")
     * endpoint per modificare l'indirizzo @PostMapping("/update-address}")
     * endpoint per vedere il codice puk     @PostMapping("/show-puk}")
     * */



}
